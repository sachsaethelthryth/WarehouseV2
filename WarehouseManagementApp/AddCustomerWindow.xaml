<Window x:Class="WarehouseManagementApp.AddCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить клиента" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Название клиента -->
        <TextBlock Text="Название клиента:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="NameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Контактная информация -->
        <TextBlock Text="Контактная информация:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox x:Name="ContactInfoTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Кнопки управления -->
        <Button Content="Сохранить" Grid.Row="3" Grid.Column="0" Margin="5" HorizontalAlignment="Right" Click="SaveButton_Click"/>
        <Button Content="Отмена" Grid.Row="3" Grid.Column="1" Margin="5" HorizontalAlignment="Left" Click="CancelButton_Click"/>
    </Grid>
</Window>